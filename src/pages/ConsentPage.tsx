import { Link } from 'react-router-dom'
import { useContent } from '@/lib/ContentProvider'
import { LEGAL_DOCUMENT_EFFECTIVE_DATE, PERSONAL_DATA_CONSENT_VERSION } from '@/lib/legal'
import { Seo } from '@/components/Seo'
import { Container, Section } from '@/components/ui/Section'
import { LegalArticle } from '@/components/LegalArticle'

/** Отдельный документ согласия для формы заявки. */
export default function ConsentPage() {
  const { settings } = useContent()
  const { legal, contacts } = settings
  const operator = legal.name || 'Оператор'

  return (
    <>
      <Seo
        title="Согласие на обработку персональных данных"
        description="Условия согласия на обработку персональных данных, предоставляемых через форму обратной связи."
        path="/soglasie-na-obrabotku-dannyh"
        noindex
      />

      <div className="bg-hero-gradient py-14 text-white sm:py-16">
        <Container>
          <h1 className="text-3xl sm:text-4xl">Согласие на обработку персональных данных</h1>
          <p className="mt-4 text-white/60">Версия {PERSONAL_DATA_CONSENT_VERSION} · действует с {LEGAL_DOCUMENT_EFFECTIVE_DATE}</p>
        </Container>
      </div>

      <Section tone="light">
        <Container>
          <LegalArticle>
            <p>
              Проставляя отдельную отметку в форме заявки, пользователь свободно, своей волей и в
              своём интересе даёт {operator}
              {legal.inn && ` (ИНН ${legal.inn})`}
              {legal.address && `, адрес: ${legal.address}`}
              {contacts.email && `, e-mail: ${contacts.email}`}
              {' '}согласие на обработку персональных данных на указанных ниже условиях.
            </p>

            <h2>1. Данные и цели</h2>
            <p>Согласие распространяется на имя, телефон, e-mail (если указан), выбранную услугу,
              комментарий, страницу отправки, дату и время обращения, номер версии согласия.</p>
            <p>Цели: ответ на обращение, консультация по услугам, расчёт стоимости, подготовка,
              заключение и исполнение договора. Согласие не является согласием на рекламные рассылки.</p>

            <h2>2. Действия с данными</h2>
            <p>
              Оператор вправе собирать, записывать, систематизировать, накапливать, хранить,
              уточнять, извлекать, использовать для ответа на обращение, блокировать, удалять и
              уничтожать данные автоматизированным и неавтоматизированным способами.
            </p>

            <h2>3. Лица, обрабатывающие данные</h2>
            <p>
              Доступ имеют Оператор и уполномоченные им лица. Для хранения обращения используется
              Firebase, для уведомления Оператора — Gmail (Google). Эти сервисы могут обрабатывать
              данные за пределами Российской Федерации. Пользователь подтверждает, что ознакомлен с
              этой информацией до предоставления согласия.
            </p>

            <h2>4. Срок и отзыв</h2>
            <p>
              Согласие действует до достижения цели обработки или отзыва. Обращение, не перешедшее в
              договор, хранится не более 180 календарных дней. Согласие можно отозвать, направив
              обращение {contacts.email ? <a href={`mailto:${contacts.email}`}>на {contacts.email}</a> : 'по контактам Оператора'}.
              После отзыва Оператор прекращает обработку и уничтожает данные, если их сохранение не
              требуется законом или заключённым договором.
            </p>

            <h2>5. Дополнительно</h2>
            <p>
              Условия обработки, права пользователя и настройка аналитики описаны в{' '}
              <Link to="/politika-konfidencialnosti">политике обработки персональных данных</Link>.
              Согласие на публикацию отзывов, фото, документов или иных данных для неограниченного
              круга лиц оформляется отдельно.
            </p>
          </LegalArticle>
        </Container>
      </Section>
    </>
  )
}
